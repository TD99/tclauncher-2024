<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:animations="clr-namespace:TCLauncher.MVVM.Animations">
    <!--
        The WPF Expander is intentionally a light-weight disclosure control: a
        ToggleButton header with a direction indicator and content below it.
        Keep that familiar structure and only add the app's dark palette.
    -->
    <Style x:Key="ModernExpander" TargetType="{x:Type Expander}">
        <Setter Property="Foreground" Value="#DCE8F7" />
        <Setter Property="Padding" Value="8,6" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="animations:ExpanderMotion.IsEnabled" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Expander}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <ToggleButton
                            x:Name="HeaderSite"
                            Padding="{TemplateBinding Padding}"
                            HorizontalContentAlignment="Stretch"
                            Background="Transparent"
                            BorderThickness="0"
                            Focusable="True"
                            IsChecked="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="{x:Type ToggleButton}">
                                    <Border
                                        x:Name="HeaderBackground"
                                        Padding="{TemplateBinding Padding}"
                                        Background="Transparent"
                                        BorderBrush="Transparent"
                                        BorderThickness="1"
                                        CornerRadius="7">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="16" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <Path
                                                x:Name="Arrow"
                                                Width="8"
                                                Height="10"
                                                HorizontalAlignment="Left"
                                                VerticalAlignment="Center"
                                                Data="M 1,1 L 6,5 L 1,9 Z"
                                                Fill="#93A9C2"
                                                SnapsToDevicePixels="True" />
                                            <ContentPresenter
                                                Grid.Column="1"
                                                VerticalAlignment="Center"
                                                Content="{Binding Header, RelativeSource={RelativeSource AncestorType={x:Type Expander}}}"
                                                ContentTemplate="{Binding HeaderTemplate, RelativeSource={RelativeSource AncestorType={x:Type Expander}}}"
                                                ContentTemplateSelector="{Binding HeaderTemplateSelector, RelativeSource={RelativeSource AncestorType={x:Type Expander}}}"
                                                TextElement.Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type Expander}}}" />
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="HeaderBackground" Property="Background" Value="#26384C" />
                                            <Setter TargetName="Arrow" Property="Fill" Value="#C8DDF3" />
                                        </Trigger>
                                        <Trigger Property="IsKeyboardFocused" Value="True">
                                            <Setter TargetName="HeaderBackground" Property="BorderBrush" Value="#76B8FF" />
                                        </Trigger>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="Arrow" Property="Data" Value="M 1,2 L 9,2 L 5,7 Z" />
                                            <Setter TargetName="Arrow" Property="Fill" Value="#8BC4FF" />
                                        </Trigger>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter Property="Opacity" Value="0.5" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>

                        <ContentPresenter
                            x:Name="ExpandSite"
                            Grid.Row="1"
                            Margin="0,4,0,0"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            ContentSource="Content"
                            Visibility="Collapsed" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource ModernExpander}" TargetType="{x:Type Expander}" />
</ResourceDictionary>
