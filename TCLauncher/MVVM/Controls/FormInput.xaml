<UserControl
    x:Class="TCLauncher.MVVM.Controls.FormInput"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:TCLauncher.MVVM.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="LocalFormInput"
    d:Background="White"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <converters:InvertibleBooleanToVisibilityConverter x:Key="InvertibleBooleanToVisibilityConverter" />
            <converters:GreaterThanZeroConverter x:Key="GreaterThanZeroConverter" />
            <converters:RequiredFieldTextConverter x:Key="RequiredFieldTextConverter" />

            <Style TargetType="PasswordBox">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="Black" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="BorderBrush" Value="#afb3b6" />
                <Setter Property="FontSize" Value="12" />
                <Setter Property="Padding" Value="10,8" />
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type PasswordBox}">
                            <Border
                                x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5"
                                SnapsToDevicePixels="True">
                                <ScrollViewer
                                    x:Name="PART_ContentHost"
                                    Focusable="False"
                                    HorizontalScrollBarVisibility="Hidden"
                                    VerticalScrollBarVisibility="Hidden" />
                            </Border>

                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Border" Property="BorderBrush" Value="{Binding ElementName=LocalFormInput, Path=AccentHoverColor}" />
                                </Trigger>

                                <Trigger Property="IsFocused" Value="True">
                                    <Setter TargetName="Border" Property="BorderBrush" Value="{Binding ElementName=LocalFormInput, Path=AccentColor}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="TextBox">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="Black" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="BorderBrush" Value="#afb3b6" />
                <Setter Property="FontSize" Value="12" />
                <Setter Property="Padding" Value="10,8" />
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type TextBoxBase}">
                            <Border
                                x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5"
                                SnapsToDevicePixels="True">
                                <ScrollViewer
                                    x:Name="PART_ContentHost"
                                    Focusable="False"
                                    HorizontalScrollBarVisibility="Hidden"
                                    VerticalScrollBarVisibility="Hidden" />
                            </Border>

                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Border" Property="BorderBrush" Value="{Binding ElementName=LocalFormInput, Path=AccentHoverColor}" />
                                </Trigger>

                                <Trigger Property="IsFocused" Value="True">
                                    <Setter TargetName="Border" Property="BorderBrush" Value="{Binding ElementName=LocalFormInput, Path=AccentColor}" />
                                </Trigger>

                                <!--  Custom Checks  -->
                                <DataTrigger Binding="{Binding ElementName=LocalFormInput, Path=IsPassword}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding ElementName=LocalFormInput, Path=IsComboBox}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding ElementName=LocalFormInput, Path=IsPassword}" Value="False" />
                                        <Condition Binding="{Binding ElementName=LocalFormInput, Path=IsComboBox}" Value="False" />
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Visibility" Value="Visible" />
                                </MultiDataTrigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="ComboBox">
                <Setter Property="ItemContainerStyle">
                    <Setter.Value>
                        <Style TargetType="ComboBoxItem">
                            <Setter Property="Foreground" Value="{Binding ElementName=LocalFormInput, Path=TextColor}" />
                            <Setter Property="BorderThickness" Value="0" />
                            <Setter Property="Padding" Value="10,8" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ComboBoxItem">
                                        <Border
                                            Margin="2"
                                            Padding="10,8"
                                            Background="{TemplateBinding Background}"
                                            CornerRadius="3">
                                            <ContentPresenter />
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>

                            <Style.Triggers>
                                <Trigger Property="IsFocused" Value="true">
                                    <Setter Property="Background" Value="{Binding ElementName=LocalFormInput, Path=AccentContrastColor}" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Setter.Value>
                </Setter>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBox">
                            <Grid>
                                <ToggleButton Focusable="False" IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ToggleButton.Template>
                                        <ControlTemplate>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>
                                                <Border
                                                    x:Name="Border"
                                                    Grid.Column="0"
                                                    Background="{Binding ElementName=LocalFormInput, Path=BackgroundColor}"
                                                    BorderBrush="{Binding ElementName=LocalFormInput, Path=AccentBlurColor}"
                                                    BorderThickness="1"
                                                    CornerRadius="5" />
                                                <Border
                                                    x:Name="SpacingBorder"
                                                    Grid.Column="0"
                                                    Margin="1"
                                                    Background="Transparent"
                                                    CornerRadius="5,0,0,5" />
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="ToggleButton.IsMouseOver" Value="True">
                                                    <Setter TargetName="Border" Property="BorderBrush" Value="{Binding ElementName=LocalFormInput, Path=AccentHoverColor}" />
                                                </Trigger>
                                                <Trigger Property="ToggleButton.IsChecked" Value="True">
                                                    <Setter TargetName="Border" Property="BorderBrush" Value="{Binding ElementName=LocalFormInput, Path=AccentColor}" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                                <ContentPresenter
                                    Name="ContentSite"
                                    Content="{TemplateBinding SelectionBoxItem}"
                                    ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                    ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                    IsHitTestVisible="False">
                                    <ContentPresenter.Resources>
                                        <Style TargetType="{x:Type TextBlock}">
                                            <Setter Property="Foreground" Value="{Binding ElementName=LocalFormInput, Path=TextColor}" />
                                            <Setter Property="Padding" Value="10,9" />
                                        </Style>
                                    </ContentPresenter.Resources>
                                </ContentPresenter>
                                <TextBox
                                    x:Name="PART_EditableTextBox"
                                    IsReadOnly="{TemplateBinding IsReadOnly}"
                                    Visibility="Hidden" />
                                <Popup
                                    Name="Popup"
                                    AllowsTransparency="True"
                                    Focusable="False"
                                    IsOpen="{TemplateBinding IsDropDownOpen}"
                                    Placement="Bottom"
                                    PopupAnimation="Slide">
                                    <Grid
                                        Name="DropDown"
                                        MinWidth="{TemplateBinding ActualWidth}"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                        SnapsToDevicePixels="True">
                                        <Border
                                            x:Name="DropDownBorder"
                                            Background="{Binding ElementName=LocalFormInput, Path=BackgroundColorSolid}"
                                            BorderBrush="{Binding ElementName=LocalFormInput, Path=AccentColor}"
                                            BorderThickness="1"
                                            CornerRadius="5" />
                                        <ScrollViewer SnapsToDevicePixels="True">
                                            <ScrollViewer.Resources>
                                                <Style x:Key="{x:Type ScrollBar}" TargetType="{x:Type ScrollBar}">
                                                    <Setter Property="Stylus.IsFlicksEnabled" Value="False" />
                                                    <Setter Property="Foreground" Value="{Binding ElementName=LocalFormInput, Path=AccentContrastColor}" />
                                                    <Setter Property="Background" Value="Transparent" />
                                                    <Setter Property="Margin" Value="0,8,3,8" />
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="{x:Type ScrollBar}">
                                                                <Grid
                                                                    x:Name="GridRoot"
                                                                    Width="8"
                                                                    Background="{TemplateBinding Background}">
                                                                    <Grid.RowDefinitions>
                                                                        <RowDefinition Height="0.00001*" />
                                                                    </Grid.RowDefinitions>

                                                                    <Track
                                                                        x:Name="PART_Track"
                                                                        Grid.Row="0"
                                                                        Focusable="False"
                                                                        IsDirectionReversed="True">
                                                                        <Track.Thumb>
                                                                            <Thumb
                                                                                x:Name="Thumb"
                                                                                Background="{TemplateBinding Foreground}"
                                                                                Style="{DynamicResource ScrollThumb}" />
                                                                        </Track.Thumb>
                                                                        <Track.IncreaseRepeatButton>
                                                                            <RepeatButton
                                                                                x:Name="PageUp"
                                                                                Command="ScrollBar.PageDownCommand"
                                                                                Focusable="False"
                                                                                Opacity="0" />
                                                                        </Track.IncreaseRepeatButton>
                                                                        <Track.DecreaseRepeatButton>
                                                                            <RepeatButton
                                                                                x:Name="PageDown"
                                                                                Command="ScrollBar.PageUpCommand"
                                                                                Focusable="False"
                                                                                Opacity="0" />
                                                                        </Track.DecreaseRepeatButton>
                                                                    </Track>
                                                                </Grid>

                                                                <ControlTemplate.Triggers>
                                                                    <Trigger SourceName="Thumb" Property="IsMouseOver" Value="True">
                                                                        <Setter TargetName="Thumb" Property="Background" Value="#CCD3DBF2" />
                                                                    </Trigger>
                                                                    <Trigger SourceName="Thumb" Property="IsDragging" Value="True">
                                                                        <Setter TargetName="Thumb" Property="Background" Value="#FFD3DBF2" />
                                                                    </Trigger>

                                                                    <Trigger Property="IsEnabled" Value="False">
                                                                        <Setter TargetName="Thumb" Property="Visibility" Value="Collapsed" />
                                                                    </Trigger>
                                                                    <Trigger Property="Orientation" Value="Horizontal">
                                                                        <Setter TargetName="GridRoot" Property="LayoutTransform">
                                                                            <Setter.Value>
                                                                                <RotateTransform Angle="-90" />
                                                                            </Setter.Value>
                                                                        </Setter>
                                                                        <Setter TargetName="PART_Track" Property="LayoutTransform">
                                                                            <Setter.Value>
                                                                                <RotateTransform Angle="-90" />
                                                                            </Setter.Value>
                                                                        </Setter>
                                                                        <Setter Property="Width" Value="Auto" />
                                                                        <Setter Property="Height" Value="12" />
                                                                        <Setter TargetName="Thumb" Property="Tag" Value="Horizontal" />
                                                                        <Setter TargetName="PageDown" Property="Command" Value="ScrollBar.PageLeftCommand" />
                                                                        <Setter TargetName="PageUp" Property="Command" Value="ScrollBar.PageRightCommand" />
                                                                    </Trigger>
                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>

                                                <Style TargetType="Thumb">
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="{x:Type Thumb}">
                                                                <Grid x:Name="Grid">
                                                                    <Rectangle
                                                                        Width="Auto"
                                                                        Height="Auto"
                                                                        HorizontalAlignment="Stretch"
                                                                        VerticalAlignment="Stretch"
                                                                        Fill="Transparent" />
                                                                    <Border
                                                                        x:Name="Rectangle1"
                                                                        Width="Auto"
                                                                        Height="Auto"
                                                                        HorizontalAlignment="Stretch"
                                                                        VerticalAlignment="Stretch"
                                                                        Background="{TemplateBinding Background}"
                                                                        CornerRadius="4" />
                                                                </Grid>
                                                                <ControlTemplate.Triggers>
                                                                    <Trigger Property="Tag" Value="Horizontal">
                                                                        <Setter TargetName="Rectangle1" Property="Width" Value="Auto" />
                                                                        <Setter TargetName="Rectangle1" Property="Height" Value="7" />
                                                                    </Trigger>
                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </ScrollViewer.Resources>

                                            <StackPanel IsItemsHost="True" />
                                        </ScrollViewer>
                                    </Grid>
                                </Popup>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <StackPanel>
        <TextBlock
            x:Name="CaptionBlock"
            Margin="0,0,0,5"
            FontSize="12"
            MouseDown="CaptionBlock_OnMouseDown">
            <TextBlock.Text>
                <MultiBinding Converter="{StaticResource RequiredFieldTextConverter}">
                    <Binding ElementName="LocalFormInput" Path="Caption" />
                    <Binding ElementName="LocalFormInput" Path="IsRequired" />
                </MultiBinding>
            </TextBlock.Text>

            <TextBlock.Style>
                <Style TargetType="{x:Type TextBlock}">
                    <Style.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding ElementName=LocalFormInput, Path=IsPassword}" Value="True" />
                                <Condition Binding="{Binding ElementName=PasswordBox, Path=IsFocused}" Value="True" />
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Foreground" Value="{Binding ElementName=LocalFormInput, Path=AccentColor}" />
                        </MultiDataTrigger>

                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding ElementName=LocalFormInput, Path=IsPassword}" Value="True" />
                                <Condition Binding="{Binding ElementName=PasswordBox, Path=IsFocused}" Value="False" />
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Foreground" Value="{Binding ElementName=LocalFormInput, Path=AccentBlurColor}" />
                        </MultiDataTrigger>

                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding ElementName=LocalFormInput, Path=IsPassword}" Value="False" />
                                <Condition Binding="{Binding ElementName=TextBox, Path=IsFocused}" Value="True" />
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Foreground" Value="{Binding ElementName=LocalFormInput, Path=AccentColor}" />
                        </MultiDataTrigger>

                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding ElementName=LocalFormInput, Path=IsPassword}" Value="False" />
                                <Condition Binding="{Binding ElementName=TextBox, Path=IsFocused}" Value="False" />
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Foreground" Value="{Binding ElementName=LocalFormInput, Path=AccentBlurColor}" />
                        </MultiDataTrigger>

                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding ElementName=LocalFormInput, Path=IsComboBox}" Value="True" />
                                <Condition Binding="{Binding ElementName=ComboBox, Path=IsDropDownOpen}" Value="True" />
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Foreground" Value="{Binding ElementName=LocalFormInput, Path=AccentColor}" />
                        </MultiDataTrigger>

                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding ElementName=LocalFormInput, Path=IsComboBox}" Value="True" />
                                <Condition Binding="{Binding ElementName=ComboBox, Path=IsDropDownOpen}" Value="False" />
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Foreground" Value="{Binding ElementName=LocalFormInput, Path=AccentBlurColor}" />
                        </MultiDataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <Grid>
            <Grid.Style>
                <Style TargetType="Grid">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ElementName=LocalFormInput, Path=IsComboBox}" Value="True">
                            <Setter Property="Cursor" Value="Arrow" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ElementName=LocalFormInput, Path=IsComboBox}" Value="False">
                            <Setter Property="Cursor" Value="IBeam" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>

            <!--  ReSharper disable Xaml.BindingWithContextNotResolved  -->
            <TextBlock
                x:Name="HintBlock"
                Margin="12,0"
                VerticalAlignment="Center"
                Panel.ZIndex="1"
                FontSize="13"
                Foreground="#8d8d8d"
                IsHitTestVisible="False"
                Text="{Binding ElementName=LocalFormInput, Path=Hint}">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Style.Triggers>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding ElementName=LocalFormInput, Path=IsPassword}" Value="True" />
                                    <Condition Binding="{Binding ElementName=LocalFormInput, Path=Password.Length, Converter={StaticResource GreaterThanZeroConverter}}" Value="False" />
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Visibility" Value="Visible" />
                            </MultiDataTrigger>

                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding ElementName=LocalFormInput, Path=IsPassword}" Value="True" />
                                    <Condition Binding="{Binding ElementName=LocalFormInput, Path=Password.Length, Converter={StaticResource GreaterThanZeroConverter}}" Value="True" />
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Visibility" Value="Collapsed" />
                            </MultiDataTrigger>

                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding ElementName=LocalFormInput, Path=IsPassword}" Value="False" />
                                    <Condition Binding="{Binding ElementName=TextBox, Path=Text.IsEmpty}" Value="True" />
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Visibility" Value="Visible" />
                            </MultiDataTrigger>

                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding ElementName=LocalFormInput, Path=IsPassword}" Value="False" />
                                    <Condition Binding="{Binding ElementName=TextBox, Path=Text.IsEmpty}" Value="False" />
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Visibility" Value="Collapsed" />
                            </MultiDataTrigger>

                            <DataTrigger Binding="{Binding ElementName=LocalFormInput, Path=IsComboBox}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
            <!--  ReSharper restore Xaml.BindingWithContextNotResolved  -->

            <TextBox x:Name="TextBox" />
            <PasswordBox
                x:Name="PasswordBox"
                PasswordChanged="OnPasswordChanged"
                Visibility="{Binding ElementName=LocalFormInput, Path=IsPassword, Converter={StaticResource InvertibleBooleanToVisibilityConverter}}" />
            <ComboBox
                x:Name="ComboBox"
                ItemsSource="{Binding ElementName=LocalFormInput, Path=ComboBoxItems}"
                Visibility="{Binding ElementName=LocalFormInput, Path=IsComboBox, Converter={StaticResource InvertibleBooleanToVisibilityConverter}}" />
        </Grid>
        <TextBlock
            FontSize="12"
            FontStyle="Italic"
            Foreground="{Binding ElementName=LocalFormInput, Path=RequirementCaptionColor}"
            Text="{Binding ElementName=LocalFormInput, Path=RequirementCaption}">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ElementName=LocalFormInput, Path=RequirementCaption}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
    </StackPanel>
</UserControl>
