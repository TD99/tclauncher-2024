<UserControl
    x:Class="TCLauncher.MVVM.Controls.SkeletonLoader"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Rectangle
            x:Name="SkeletonRectangle"
            d:DataContext="{d:DesignInstance}"
            Fill="{Binding Background}"
            RadiusX="{Binding BorderRadius}"
            RadiusY="{Binding BorderRadius}">
            <Rectangle.RenderTransform>
                <RotateTransform Angle="{Binding Rotation}" />
            </Rectangle.RenderTransform>
            <Rectangle.Triggers>
                <EventTrigger RoutedEvent="Rectangle.Loaded">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation
                                AutoReverse="True"
                                RepeatBehavior="Forever"
                                Storyboard.TargetName="SkeletonRectangle"
                                Storyboard.TargetProperty="Opacity"
                                From="1.0"
                                To="0.5"
                                Duration="{Binding AnimationDuration}" />
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Rectangle.Triggers>
        </Rectangle>
    </Grid>
</UserControl>
