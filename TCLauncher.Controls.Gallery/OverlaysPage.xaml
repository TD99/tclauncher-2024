<UserControl
    x:Class="TCLauncher.Controls.Gallery.OverlaysPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:TCLauncher.MVVM.Controls;assembly=TCLauncher">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <WrapPanel Grid.Row="0" Margin="0,0,0,22">
            <Button Margin="0,0,10,10" Content="Show drawer" Click="ShowDrawer_OnClick" />
            <Button Margin="0,0,10,10" Content="Show sheet" Click="ShowSheet_OnClick" />
            <Button Margin="0,0,10,10" Content="Show confirmation" Click="ShowConfirmation_OnClick" />
        </WrapPanel>
        <Border
            Grid.Row="1"
            MinHeight="300"
            Background="#202A38"
            BorderBrush="#43546A"
            BorderThickness="1"
            CornerRadius="10">
            <Grid>
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock HorizontalAlignment="Center" FontSize="18" FontWeight="SemiBold" Text="OverlayHost playground" />
                    <TextBlock
                        Margin="0,8,0,0"
                        Foreground="#AEB8C7"
                        Text="Use the controls above to open each overlay type."
                        TextWrapping="Wrap" />
                </StackPanel>
                <controls:OverlayHost x:Name="OverlayHost" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
